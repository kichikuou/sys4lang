class C {
public:
	C() { this.m = 1; }
	int m;
private:
	int n;
};

C::set() {
	// m = 42;
	this.m = 42;
}

int main()
{
	C f;
	assert(f.m == 1);
	f.set();
	assert(f.m == 42);
	f.n = 42;  // FIXME: should cause a compile error

	return 0;
}
