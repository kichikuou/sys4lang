int result;
int a_called;

void message(int n, int total, string msg)
{
	result = msg == "test";
}

void A(void)
{
	a_called = 1;
}

int main()
{
	'test' A;
	assert(result == 1);
	assert(a_called == 1);
}
